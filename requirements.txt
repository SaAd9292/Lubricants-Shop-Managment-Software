# LubriPOS runtime dependencies
# Target: Python 3.13+
PySide6>=6.7,<7.0       # Desktop GUI
reportlab>=4.0          # PDF invoices & report exports
openpyxl>=3.1           # Excel (.xlsx) exports

# Password hashing uses the standard library (hashlib.pbkdf2_hmac).
# No external crypto dependency required.
# Optional future upgrade: argon2-cffi  (drop-in via core/security.py)
